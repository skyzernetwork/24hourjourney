---
import Layout from "@/layouts/Layout.astro";
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import { getPostImage } from '@/lib/getPostImage';

const allPosts = (await getCollection('case-studies').catch(() => []))
  .filter(p => !p.data.draft)
  .sort((a, b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());

const featured = allPosts.find(p => p.data.featured) ?? allPosts[0];

const postsWithImages = await Promise.all(
  allPosts.map(async (post) => ({
    post,
    coverImage: post.data.cover ? await getPostImage(post.data.cover) : null,
  }))
);

const featuredData = postsWithImages.find(({ post }) => post === featured);
const restWithImages = postsWithImages.filter(({ post }) => post !== featured);

const subMeta: Record<string, { label: string; colorClass: string }> = {
  'research':     { label: 'Research',     colorClass: 'tag-c' },
  'case-study':   { label: 'Case Study',   colorClass: 'tag-c' },
  'analysis':     { label: 'Analysis',     colorClass: 'tag-m' },
  'industry':     { label: 'Industry',     colorClass: 'tag-a' },
  'deep-dive':    { label: 'Deep Dive',    colorClass: 'tag-w' },
};
---

<Layout
  title="Case Studies | 24 Hour Journey"
  description="In-depth research, analysis, and case studies on web design, digital strategy, and business growth from Skyzer Networks."
  url="https://24hourjourney.blog/case-studies"
  image_preview="/icons/preview.png"
  type="website"
>
  <!-- PAGE HEADER -->
  <header class="page-header">
    <div class="header-bg"></div>
    <div class="header-inner">
      <div class="breadcrumb"><a href="/">Home</a><span>/</span><span>Case Studies</span></div>
      <div class="header-tag">CASE STUDIES</div>
      <h1 class="header-title">Deep dives.<br/><span class="accent-text">Real findings.</span></h1>
      <p class="header-desc">Long-form research and in-depth analysis on what actually works in web design, digital strategy, and business growth.</p>
      <div class="header-stat">
        <span class="stat-num">{allPosts.length}</span>
        <span class="stat-label">studies</span>
      </div>
    </div>
  </header>

  <main class="page-main">

    {allPosts.length === 0 && (
      <div class="empty-state">No case studies yet — check back soon.</div>
    )}

    <!-- FEATURED AS FULL-WIDTH HERO -->
    {featured && featuredData && (
      <section class="featured-section">
        <div class="section-eyebrow">Latest Study</div>
        <a href={`/case-studies/${featured.id}`} class="featured-post">
          <div class="fp-img">
            {featuredData.coverImage ? (
              <Image src={featuredData.coverImage} alt={featured.data.title} class="fp-cover" />
            ) : (
              <div class="fp-img-placeholder">
                <span>01</span>
              </div>
            )}
            <div class="fp-gradient"></div>
          </div>
          <div class="fp-content">
            <div class="fp-sub">
              {featured.data.subcategory && (
                <span class={`tag ${subMeta[featured.data.subcategory]?.colorClass ?? 'tag-c'}`}>
                  {subMeta[featured.data.subcategory]?.label ?? featured.data.subcategory}
                </span>
              )}
            </div>
            <h2 class="fp-title">{featured.data.title}</h2>
            <p class="fp-excerpt">{featured.data.description}</p>
            <div class="fp-footer">
              <span>{featured.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</span>
              <span class="fp-cta">Read the full study →</span>
            </div>
          </div>
        </a>
      </section>
    )}

    <!-- REST: numbered editorial list -->
    {restWithImages.length > 0 && (
      <section class="studies-section">
        <div class="section-label">More Studies</div>
        <div class="studies-list">
          {restWithImages.map(({ post, coverImage }, i) => {
            const sub = subMeta[post.data.subcategory ?? ''] ?? { label: 'Case Study', colorClass: 'tag-c' };
            return (
              <a href={`/case-studies/${post.id}`} class="study-row">
                <div class="sr-num">{String(i + 2).padStart(2, '0')}</div>
                <div class="sr-img">
                  {coverImage ? (
                    <Image src={coverImage} alt={post.data.title} class="sr-cover" />
                  ) : (
                    <div class="sr-placeholder"></div>
                  )}
                </div>
                <div class="sr-body">
                  <div class="sr-tags">
                    <span class={`tag ${sub.colorClass}`}>{sub.label}</span>
                  </div>
                  <h3 class="sr-title">{post.data.title}</h3>
                  <p class="sr-excerpt">{post.data.description}</p>
                  <div class="sr-meta">
                    <span>{post.data.pubDate.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</span>
                    <span>Long read</span>
                  </div>
                </div>
                <div class="sr-arrow">→</div>
              </a>
            );
          })}
        </div>
      </section>
    )}

  </main>
</Layout>

<style>
/* ── PAGE HEADER ── */
.page-header {
  position: relative;
  overflow: hidden;
  padding: 4rem 1.25rem 3rem;
  margin-top: var(--space-2xl);
  border-bottom: 0.5px solid var(--border);
}

.header-bg {
  position: absolute;
  inset: 0;
  background:
    radial-gradient(ellipse 70% 60% at 110% 50%, rgba(0,200,255,0.07) 0%, transparent 70%),
    repeating-linear-gradient(
      -45deg,
      transparent, transparent 59px,
      rgba(0,200,255,0.025) 59px,
      rgba(0,200,255,0.025) 60px
    );
  pointer-events: none;
}

.header-inner {
  position: relative;
  z-index: 1;
  max-width: 1160px;
  margin: 0 auto;
}

.breadcrumb {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
  margin-bottom: 1.5rem;
}

.breadcrumb a { color: var(--muted); text-decoration: none; }
.breadcrumb a:hover { color: var(--blue); }
.breadcrumb span:last-child { color: var(--blue); }

.header-tag {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  letter-spacing: 3px;
  color: var(--blue);
  text-transform: uppercase;
  margin-bottom: 1rem;
  display: flex;
  align-items: center;
  gap: 10px;
}

.header-tag::before {
  content: '';
  width: 28px;
  height: 1px;
  background: var(--blue);
}

.header-title {
  font-family: var(--font-title);
  font-size: var(--text-4xl);
  line-height: 1;
  letter-spacing: 1px;
  color: var(--white);
  margin-bottom: 1rem;
}

.accent-text {
  background: linear-gradient(90deg, var(--blue), #cc66ff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.header-desc {
  font-size: var(--text-sm);
  color: rgba(245,243,239,0.55);
  max-width: 520px;
  line-height: 1.7;
  font-weight: 300;
  margin-bottom: 1.5rem;
}

.header-stat {
  display: flex;
  align-items: baseline;
  gap: 6px;
}

.stat-num {
  font-family: var(--font-title);
  font-size: var(--text-3xl);
  color: var(--blue);
  line-height: 1;
}

.stat-label {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
  letter-spacing: 1px;
  text-transform: uppercase;
}

/* ── MAIN ── */
.page-main {
  max-width: 1160px;
  margin: 0 auto;
  padding: 0 1.25rem 4rem;
}

.empty-state {
  font-size: var(--text-sm);
  color: var(--muted);
  font-family: var(--font-display);
  padding: 4rem 0;
  text-align: center;
}

/* ── FEATURED ── */
.featured-section {
  padding: 2.5rem 0 2.5rem;
  border-bottom: 0.5px solid var(--border);
  margin-bottom: 2.5rem;
}

.section-eyebrow {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--blue);
  margin-bottom: 1.25rem;
}

.featured-post {
  display: flex;
  flex-direction: column;
  border: 0.5px solid rgba(0,200,255,0.2);
  border-radius: 8px;
  overflow: hidden;
  text-decoration: none;
  background: var(--card-bg);
  transition: border-color 0.2s;
}

.featured-post:hover { border-color: rgba(0,200,255,0.45); }

.fp-img {
  position: relative;
  height: 260px;
  background: #001a20;
  overflow: hidden;
}

.fp-cover {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.fp-gradient {
  position: absolute;
  inset: 0;
  background: linear-gradient(to bottom, transparent 50%, rgba(0,8,12,0.7));
}

.fp-img-placeholder {
  width: 100%;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #001a20, #001018);
}

.fp-img-placeholder span {
  font-family: var(--font-title);
  font-size: 5rem;
  letter-spacing: 4px;
  color: rgba(0,200,255,0.08);
}

.fp-content {
  padding: 1.75rem;
}

.fp-sub { margin-bottom: 0.75rem; }

.fp-title {
  font-family: var(--font-sub);
  font-size: var(--text-2xl);
  line-height: 1.2;
  color: var(--white);
  margin-bottom: 0.75rem;
  letter-spacing: 0.5px;
}

.fp-excerpt {
  font-size: var(--text-sm);
  color: rgba(245,243,239,0.55);
  font-weight: 300;
  line-height: 1.7;
  margin-bottom: 1.25rem;
}

.fp-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
  border-top: 0.5px solid var(--border);
  padding-top: 1rem;
}

.fp-cta { color: var(--blue); }

/* ── STUDIES LIST ── */
.section-label {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--muted);
  margin-bottom: 1.25rem;
  display: flex;
  align-items: center;
  gap: 10px;
}

.section-label::after {
  content: '';
  flex: 1;
  height: 0.5px;
  background: var(--border);
}

.studies-list {
  display: flex;
  flex-direction: column;
}

.study-row {
  display: flex;
  align-items: flex-start;
  gap: 1rem;
  padding: 1.25rem 0;
  border-bottom: 0.5px solid var(--border);
  text-decoration: none;
  transition: background 0.15s;
  border-radius: 4px;
}

.study-row:last-child { border-bottom: none; }
.study-row:hover { background: rgba(0,200,255,0.02); }

.sr-num {
  font-family: var(--font-title);
  font-size: var(--text-2xl);
  color: rgba(0,200,255,0.12);
  width: 36px;
  flex-shrink: 0;
  line-height: 1;
  padding-top: 4px;
}

.sr-img {
  width: 90px;
  height: 65px;
  border-radius: 4px;
  overflow: hidden;
  flex-shrink: 0;
  background: #001a20;
}

.sr-cover {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.sr-placeholder {
  width: 100%;
  height: 100%;
  background: linear-gradient(135deg, #001a20, #000f18);
}

.sr-body {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
}

.sr-tags { display: flex; gap: 5px; }

.sr-title {
  font-family: var(--font-sub);
  font-size: var(--text-md);
  color: var(--white);
  font-weight: 500;
  line-height: 1.35;
}

.sr-excerpt {
  font-size: var(--text-sm);
  color: rgba(245,243,239,0.45);
  font-weight: 300;
  line-height: 1.55;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.sr-meta {
  display: flex;
  gap: 10px;
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
}

.sr-arrow {
  font-size: var(--text-md);
  color: rgba(0,200,255,0.3);
  flex-shrink: 0;
  padding-top: 4px;
  transition: color 0.2s, transform 0.2s;
}

.study-row:hover .sr-arrow {
  color: var(--blue);
  transform: translateX(3px);
}

/* Tags */
.tag {
  display: inline-block;
  font-size: var(--text-xs);
  font-family: var(--font-display);
  padding: 3px 8px;
  border-radius: 2px;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  border: 0.5px solid var(--border);
  background: rgba(255,255,255,0.05);
  color: var(--muted);
}

.tag-c { border-color: rgba(0,200,255,0.3);   color: var(--blue);    background: rgba(0,200,255,0.05); }
.tag-m { border-color: rgba(204,0,255,0.3);   color: #cc66ff;        background: rgba(204,0,255,0.05); }
.tag-g { border-color: rgba(0,255,136,0.3);   color: #00dd77;        background: rgba(0,255,136,0.05); }
.tag-a { border-color: rgba(255,170,0,0.3);   color: #ffaa00;        background: rgba(255,170,0,0.05); }
.tag-w { border-color: rgba(200,200,200,0.2); color: #c8c8c8;        background: rgba(200,200,200,0.04); }

/* ── MEDIA QUERIES ── */

/* sm — large phones (480px+) */
@media (min-width: 480px) {
  .sr-img {
    width: 110px;
    height: 78px;
  }
}

/* md — tablets (768px+) */
@media (min-width: 768px) {
  .page-header { padding: 5rem 2rem 3.5rem; }
  .header-title { font-size: var(--text-5xl); }
  .page-main { padding: 0 2rem 4rem; }

  .featured-post {
    flex-direction: row;
  }

  .fp-img {
    width: 420px;
    height: auto;
    flex-shrink: 0;
  }

  .fp-content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .fp-title { font-size: var(--text-3xl); }

  .sr-img {
    width: 130px;
    height: 90px;
  }

  .sr-excerpt { -webkit-line-clamp: 3; }
}

/* lg — laptops (1024px+) */
@media (min-width: 1024px) {
  .sr-num { font-size: var(--text-3xl); width: 44px; }

  .sr-img {
    width: 150px;
    height: 100px;
  }
}

/* xl — standard desktops (1280px+) */
@media (min-width: 1280px) {
  .fp-img { width: 500px; }
}
</style>
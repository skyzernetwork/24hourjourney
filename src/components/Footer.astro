<footer class="footer">
  <div class="footer-inner">

    <div class="footer-grid">

      <!-- Brand -->
      <div class="footer-brand">
        <div class="footer-logo">
          <span class="footer-logo-dot"></span>
          24HOURJOURNEY<span class="footer-logo-ext">.blog</span>
        </div>
        <p class="footer-brand-desc">
          Honest writing about websites, design, and the web — for small business owners and creators who just want things to work.
        </p>
        <div class="footer-social">
          <a href="https://skyzernetworks.com" class="footer-social-link" aria-label="Skyzer Networks">SN</a>
          <a href="https://www.instagram.com/skyzernetworks/" class="footer-social-link" aria-label="Instagram">IG</a>
          <a href="https://x.com/skyzernetworks" class="footer-social-link" aria-label="Twitter/X">X</a>
          <a href="https://www.facebook.com/profile.php?id=61588085351746" class="footer-social-link" aria-label="Facebook">FB</a>
        </div>
      </div>

      <!-- Links -->
      <div class="footer-cols">
        <div class="footer-col">
          <h4 class="footer-col-title">Content</h4>
          <ul class="footer-col-list">
            <li><a href="/posts">All posts</a></li>
            <li><a href="/case-studies">Case studies</a></li>
            <li><a href="/guides">Guides</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4 class="footer-col-title">Topics</h4>
          <ul class="footer-col-list">
            <li><a href="/for-business">For Business</a></li>
            <li><a href="/design">Design</a></li>
            <li><a href="/portfolios">Portfolios</a></li>
            <li><a href="/behind-the-build">Behind the Build</a></li>
          </ul>
        </div>

        <div class="footer-col">
          <h4 class="footer-col-title">Site</h4>
          <ul class="footer-col-list">
            <li><a href="/about">About</a></li>
            <li><a href="/rss.xml">RSS Feed</a></li>
            <li><a href="https://skyzernetworks.com" target="_blank" rel="noopener">Skyzer Networks</a></li>
            <li><a href="/contact">Contact</a></li>
          </ul>
        </div>
      </div>

    </div>

    <div class="footer-bottom">
      <span class="footer-copy">© 2026 24hourjourney.blog — A Skyzer Networks project</span>
      <div class="footer-legal">
        <a href="/privacy">Privacy</a>
        <span class="footer-sep">·</span>
        <a href="/terms">Terms</a>
      </div>
    </div>

  </div>
</footer>

<style>
.footer {
  background: #060606;
  border-top: 0.5px solid rgba(255, 255, 255, 0.08);
}

.footer-inner {
  max-width: 1160px;
  margin: 0 auto;
  padding: 2.5rem 1.25rem 1.5rem;
}

/* ── GRID ── */
.footer-grid {
  display: flex;
  flex-direction: column;
  gap: 2rem;
  margin-bottom: 2rem;
}

/* ── BRAND ── */
.footer-logo {
  font-family: var(--font-title);
  font-size: 22px;
  letter-spacing: 2px;
  color: var(--white);
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 0.75rem;
}

.footer-logo-dot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: var(--blue);
  flex-shrink: 0;
}

.footer-logo-ext {
  font-family: var(--font-display);
  font-size: 13px;
  color: var(--muted);
  letter-spacing: 0;
  font-weight: 400;
}

.footer-brand-desc {
  font-size: var(--text-sm);
  color: var(--muted);
  font-weight: 300;
  line-height: 1.7;
  max-width: 300px;
  margin-bottom: 1.25rem;
}

.footer-social {
  display: flex;
  gap: 8px;
}

.footer-social-link {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
  text-decoration: none;
  border: 0.5px solid rgba(255, 255, 255, 0.08);
  padding: 5px 12px;
  border-radius: 3px;
  letter-spacing: 0.5px;
  transition: color 0.2s, border-color 0.2s;
}

.footer-social-link:hover {
  color: var(--blue);
  border-color: rgba(0, 200, 255, 0.3);
}

/* ── COLS ── */
.footer-cols {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.5rem 1rem;
}

.footer-col-title {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
  letter-spacing: 1.5px;
  text-transform: uppercase;
  margin-bottom: 0.9rem;
}

.footer-col-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.footer-col-list a {
  font-size: var(--text-sm);
  color: rgba(245, 243, 239, 0.45);
  text-decoration: none;
  transition: color 0.15s;
}

.footer-col-list a:hover {
  color: var(--white);
}

/* ── BOTTOM ── */
.footer-bottom {
  border-top: 0.5px solid rgba(255, 255, 255, 0.08);
  padding-top: 1.25rem;
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.footer-copy {
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
}

.footer-legal {
  display: flex;
  align-items: center;
  gap: 8px;
  font-family: var(--font-display);
  font-size: var(--text-xs);
  color: var(--muted);
}

.footer-legal a {
  color: var(--muted);
  text-decoration: none;
  transition: color 0.15s;
}

.footer-legal a:hover {
  color: var(--blue);
}

.footer-sep {
  opacity: 0.4;
}

/* ── sm — large phones (480px+) ── */
@media (min-width: 480px) {
  .footer-cols {
    grid-template-columns: 1fr 1fr 1fr;
  }
}

/* ── md — tablets (768px+) ── */
@media (min-width: 768px) {
  .footer-inner {
    padding: 3rem 2rem 1.75rem;
  }

  .footer-bottom {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }
}

/* ── lg — laptops (1024px+) ── */
@media (min-width: 1024px) {
  .footer-grid {
    flex-direction: row;
    justify-content: space-between;
    gap: 3rem;
  }

  .footer-brand {
    flex-shrink: 0;
    width: 240px;
  }

  .footer-cols {
    flex: 1;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem 2rem;
  }
}

/* ── xl — standard desktops (1280px+) ── */
@media (min-width: 1280px) {
  .footer-inner {
    padding: 3.5rem 2.5rem 2rem;
  }

  .footer-brand {
    width: 280px;
  }
}
</style>